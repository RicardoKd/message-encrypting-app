/**
 * Interface to model the Account Schema for TypeScript
 * @param id:number
 * @param name:string
 * @param password:string
 */
export interface IAccount {
  _id: number;
  name: string;
  password: string;
}
